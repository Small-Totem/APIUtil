<?xml version="1.0" encoding="utf-8"?>
<!--注意,hide的时候没有加动画,所以这个其实不会出现 (于FragmentSwitcher.show_fragment_by_tag(String tag))
因为fragment可能不一样高,如果用这个的话之前的fragment可能会闪现一下-->
<alpha xmlns:android="http://schemas.android.com/apk/res/android"
    android:duration="300"
    android:fromAlpha="1.0"
    android:toAlpha="0"
/>
